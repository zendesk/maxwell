language: java
env:
  - MYSQL_VERSION=5.5
  - MYSQL_VERSION=5.6
  - AGRESSIVE_RECOVERY_TEST=1 MYSQL_VERSION=5.5
before_install:
  - sudo apt-get -qq update
  - sudo apt-get install pastebinit
install: true
script:
  - make test
after_script:
  - pastebinit -i test.log
